<?xml version="1.0" encoding="utf-8"?>
<!--
  Requests the minimum needed to inject input: gesture dispatch, the global
  navigation actions, and retrieval of the focused node so text can be typed.
  No event types are subscribed to, because this service never observes the UI.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canPerformGestures="true"
    android:canRetrieveWindowContent="true"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100"
    android:summary="@string/accessibility_service_label" />
