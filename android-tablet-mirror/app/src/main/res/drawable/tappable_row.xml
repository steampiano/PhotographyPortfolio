<?xml version="1.0" encoding="utf-8"?>
<!--
  Backdrop for a row that is meant to be tapped.

  Device names were previously bare text in a list, which reads as a label rather
  than a control — in the unpair dialog especially, nothing distinguished "here is
  the tablet you paired with" from "tap this to unpair it". An outline and a ripple
  make the target visible, and the rounded corners match the viewer's control bar so
  the two look like the same app.

  The ripple is drawn over the outline rather than replacing it, so the row still
  looks tappable when it is not being touched.
-->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">

    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <corners android:radius="12dp" />
            <stroke
                android:width="1dp"
                android:color="?attr/colorOutline" />
        </shape>
    </item>

</ripple>
